// Definimos una función llamada 'sum' que recibe dos valores: a y b
function sum(a, b) {
    // La función devuelve la suma de a + b
    return a + b;
  }
  
  // Exportamos la función para poder usarla en otros archivos
  // (por ejemplo, en nuestro archivo de pruebas)
  module.exports = sum;
  
  